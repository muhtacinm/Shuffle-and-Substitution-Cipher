public interface MessageDecoder {
    public abstract String decode(String cipherText);  // abstract method which decodes the given
                                                        // cipherText into decrypted text and returns the decrypted text

}
